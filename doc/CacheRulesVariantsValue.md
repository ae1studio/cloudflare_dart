# cloudflare_dart.model.CacheRulesVariantsValue

## Load the model package
```dart
import 'package:cloudflare_dart/api.dart';
```

## Properties
Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------
**avif** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for avif. | [optional] 
**bmp** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for bmp. | [optional] 
**gif** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for gif. | [optional] 
**jp2** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for jp2. | [optional] 
**jpeg** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for jpeg. | [optional] 
**jpg** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for jpg. | [optional] 
**jpg2** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for jpg2. | [optional] 
**png** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for png. | [optional] 
**tif** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for tif. | [optional] 
**tiff** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for tiff. | [optional] 
**webp** | **BuiltSet&lt;String&gt;** | List of strings with the MIME types of all the variants that should be served for webp. | [optional] 

[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


